# Build output (workspace arrives with CX-2)
/target

# Local research and scratch material — must never be committed
/notes/
/research/
/scratch/
*.local.md

# Website (Docusaurus) — build artefacts and dependencies
/website/node_modules/
/website/build/
/website/.docusaurus/
/website/.cache-loader/
npm-debug.log*
yarn-error.log*
